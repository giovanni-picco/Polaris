apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-planka-app
  namespace: flux-system
spec:
  interval: 5m
  path: ./k8s/apps/openspeedtest/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: apps-openspeedtest-db
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-config