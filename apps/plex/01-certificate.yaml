---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: plex-certificate
  namespace: plex
spec:
  secretName: plex-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: plex.${CLUSTER_DOMAIN}
  dnsNames:
    - plex.${CLUSTER_DOMAIN}