---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: intel-gpu
  namespace: flux-system
spec:
  interval: 1m0s
  url: https://gitlab.com/gp-homelab/k8s/intel-gpu.git
  ref:
    branch: main
  secretRef:
    name: gitlab-deploy-token
